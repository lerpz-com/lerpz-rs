[package]
name = "auth"
version = "0.1.0"
edition = "2021"

[dependencies]
core = { path = "../core", features = ["serde"]}
chrono = "0.4"
jsonwebtoken = "9.2"
once_cell = "1.19"
rand = "0.8"
serde = "1.0"
uuid = { version = "1.6", features = ["v4", "serde"] }
